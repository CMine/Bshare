# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base             # Packages every Meteor app needs to have
mobile-experience       # Packages for a great mobile UX
mongo                   # The database Meteor supports right now
blaze-html-templates    # Compile .html files into Meteor Blaze views
session                 # Client-side reactive dictionary for your app
jquery                  # Helpful client-side library
tracker                 # Meteor's client-side reactive programming library

es5-shim                # ECMAScript 5 compatibility for older browsers.
ecmascript              # Enable ECMAScript2015+ syntax in app code

meteortoys:allthings    # In-app dev tools: Autopub, Authenticate, Throttle
kadira:flow-router      # Client Side Router
kadira:blaze-layout     # Layout manager designed for Blaze

fourseven:scss          # Sass build plugin for Meteor
fortawesome:fontawesome # Fontawesome for Meteor (obsolete?)
standard-minifier-css   # Minifier plugin used for Meteor
standard-minifier-js    # Minifier plugin used for Meteor

accounts-base           # Meteor's user account system
accounts-password       # Meteor's password system
useraccounts:unstyled   # Accounts Templates Unstyled
raix:handlebar-helpers  # Handlebar extension helper
alanning:roles          # Extends user account system with special roles
chart:chart             # Wrapper for Chart.js
aldeed:collection2      # Automatic validation of insert and update
reactive-dict           # Reactive dictionary

reactive-var            # Reactive Variable
check                   # Check if a value matches a pattern
momentjs:moment         # Parse, Validate, and Manipulate dates
twbs:bootstrap          # Bootstrap Wrapper
semantic:ui-card        # Semantic UI Wrapper (Cards)
semantic:ui-button      # Semantic UI Wrapper (Buttons)
semantic:ui-icon        # Semantic UI Wrapper (Icons)
semantic:ui-grid        # Semantic UI Wrapper (Grid)
semantic:ui-form        # Semantic UI Wrapper (Forms)
semantic:ui-dropdown    # Semantic UI Wrapper (Dropdown)
